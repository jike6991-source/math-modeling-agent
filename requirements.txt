# ===== Phase 1（最小pipeline）=====
openai           # 调用 DeepSeek API（openai 兼容格式）
python-dotenv    # 加载 .env 配置
matplotlib       # 图表生成（中文字体 SimHei）
python-docx      # 论文输出（docx）
pytest           # 测试
autopep8         # 执行前格式化 reasoner 生成的代码（修复缩进错误）

# ===== 建模求解 =====
pulp             # 整数/线性规划求解
ortools          # CP-SAT 约束规划（排班/调度类问题）
statsmodels      # 时间序列（ARIMA 等）

# ===== Streamlit 前端 =====
streamlit        # Web UI 框架
pandas           # CSV/Excel 预览
openpyxl         # pandas 读取 .xlsx 所需引擎

# ===== Phase 2（RAG）=====
chromadb              # 向量数据库（持久化到 knowledge/chroma_db）
sentence-transformers # BGE-small-zh-v1.5 embeddings
PyMuPDF               # PDF 文本提取（import fitz）
